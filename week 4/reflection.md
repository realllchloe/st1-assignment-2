Before using AI, I ran the Task 1 and Enhanced Task 1 code, which is the SmartCare system prototype. It shows how the patient names, practitioner names, and appointment times are stored in dictionaries. Then, the appointment records are stored in a list, using append ( ) to allow more appointments to be added. It also uses defined functions to create appointments and display appointment records. I also recognised that there are some limitations in validation and booking conflicts.

I can understand the concepts of dictionaries and lists, but I am weak at using them. AI helped me understand how to use them and suggested improvements for the limitations of the prototype, even though not all the suggestions were acceptable.

The prototype already assumes that the patient’s name, practitioner name, and appointment time are strings. The AI version continued to use this assumption when improving the prototype, so this was not a new assumption made by AI.

I ran the AI code and used:

A normal appointment to test whether the code works correctly.

A blank patient name, patient_name=None, and appointment_time=None to test the validation.

Two appointments for the same practitioner and time to test the booking conflict.

I also checked whether the code followed the prompt.

The engineering work that remained for me was to decide whether the AI version was workable and what needed to be improved. I also needed to check for any assumptions because we must follow the business rules, decide which parts of the code to use, and take responsibility for the final code.
